/** Autosave debounce — tuned for fewer API calls while typing feels instant. */
export const AUTOSAVE_DEBOUNCE_MS = 1400;

/** Shorter debounce after manual save so rapid edits still batch. */
export const AUTOSAVE_DEBOUNCE_AFTER_MANUAL_MS = 900;
